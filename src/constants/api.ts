export const DEFAULT_RETRIEVAL_LIMIT = 10;

export enum DREAM_PATH {
    DREAMS = '/dream',
    DREAM = '/dream/:dreamId'
}

export enum USER_PATH {
    USERS = '/user',
    PROFILE = '/user/:userId/profile',
}